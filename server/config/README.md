# Config placeholders

This directory centralizes backend configuration helpers (e.g., env loaders, future feature flags).
It intentionally does not hardcode secrets; all sensitive values must come from environment variables.
TODO: add typed config helpers once the Postgres + Mongo connections are fully wired.


