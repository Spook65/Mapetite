# Environment template for backend connectivity.
# Secrets stay out of git; copy to .env locally and fill in per developer.

# Primary relational store (Postgres)
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/mapetite"

# Optional cache/experimental store (MongoDB)
# The app must still run if this is empty or offline.
MONGODB_URI="mongodb://localhost:27017/mapetite_cache"

# External API keys (never commit real values)
PLACES_API_KEY="replace-me"
OTHER_API_KEY="replace-me"


